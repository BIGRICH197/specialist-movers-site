/** Half-width photo column , matches homepage hero couch / brand moments */
export const halfPhotoWrap =
  "w-full max-w-xl sm:max-w-2xl lg:max-w-[min(100%,40rem)]";

/** Centered block for beside photo + caption (not full-bleed on wide screens) */
export const halfPhotoSectionWrap = "mx-auto w-full max-w-5xl";
