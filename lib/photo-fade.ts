/** Photo frame classes , defined in globals.css (outside Tailwind @layer) */

/** Light purple wash + bottom fade on most photos */
export const photoFrameClass = "photo-frame";

/** Stronger bottom purple for yellow caption on image */
export const photoFrameCaptionClass = "photo-frame photo-frame--caption";

/** Caption fade without top-left purple wash (hero title on photo) */
export const photoFrameCaptionTopClearClass =
  "photo-frame photo-frame--caption photo-frame--caption-top-clear";

/** Bottom caption fade only, no top purple wash */
export const photoFrameCaptionBottomOnlyClass =
  "photo-frame photo-frame--caption photo-frame--caption-bottom-only";

/** Round mug shots on About team */
export const photoFrameRoundClass = "photo-frame photo-frame--round";
