# Site photos

**60 originals** copied from your WeTransfer downloads:

- `source/batch-p125/` , 31 files (P125xxxx)
- `source/batch-p126-p127/` , 29 files (P126/P127)

**Which photo goes where** is set in `lib/site-photos.ts`. Change a filename there to swap a hero or service card without touching page code.

First-load may be slow while Next.js optimises large JPGs (~5–8 MB each). We can compress a web-sized set later if needed.
